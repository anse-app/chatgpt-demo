---
import ConversationSidebarItem from './ConversationSidebarItem.astro'
---

<aside class="w-[300px] h-full border-r">
  <div class="h-12 border-b flex px-4 items-center">
    Conversations
  </div>
  { Array.from({ length: 4 }).map((_, i) => (
    <ConversationSidebarItem key={i} />
  ))}
</aside>